#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/*determine if the number is positive, negative or zero */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	if (n>o)
	{
		printf(n, "n is positive")
	}
	else (if n==o)
	{
	       printf(n, "n is zero")
	}
	else (if n<0)
	{
		printf(n,  "n is negative")
	}
	printf(a,"n is : %d\n")
				      
	return (0);
}
